高德地图 JS API（v2.0）
AMap.Map：初始化地图
AMap.Marker：标记点位
AMap.TileLayer.Traffic：路况图层开关
路线规划服务（根据模式自动调用）：AMap.Driving、AMap.Walking、AMap.Riding
map.setFitView()、map.setCenter()、map.setZoom() 等视角控制
AMap.Geolocation：定位到我（浏览器定位权限）
